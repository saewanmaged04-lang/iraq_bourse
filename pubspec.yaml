name: iraq_bourse
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  url_launcher: ^6.2.5
  shared_preferences: ^2.2.3
  youtube_player_flutter: ^8.1.2 # 🔹 زیادکردنی لێدەری فەرمی و جێگیری یوتیوب لێرەدا

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/